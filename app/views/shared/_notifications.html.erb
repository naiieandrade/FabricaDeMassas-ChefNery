<% cont = count_pending_orders %>

<% if cont != 1 %>
	<%= link_to "#{cont} pedidos pendentes", orders_path, remote: true  %>
<% else %>
	<%= link_to "#{cont} pedido pendente", orders_path, remote: true %>
<% end %>
